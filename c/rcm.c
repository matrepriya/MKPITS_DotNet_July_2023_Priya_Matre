# include <stdio.h>

# include <conio.h>
int main ()
{
	int m1,m2,m3, total;
	float per;
	clrscr();
	printf (" each 3 subject marks");
	scanf ("%d%d%d",&m1,&m2,&m3);
 	total= m1+m2+m3;
	per= (total/300.0 f)*10000 f;
	if (per>=75)
	{ 
	printf (" grade =distinction");
	}
	else if (per>=60&& per<=75)
    {
    	printf (" \n grade= first");
    }
	else if (per >=40 && per<60);
	{
		printf (" \n grade =scored");
	}
	else
   }
   printf ("2\n fail");
   }
   return 0;
}
	 
   
