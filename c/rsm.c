# include <stdio.h>
void main()
 {
 	{char name [10];
 	int salary, bonus =0;
 	printf ("enter name");
 	scanf (" %&",& name);
 	printf("enter basic");
 	scanf("%d," & name);
 	if(salary >= 10000)
	 {
	 	bonus =2000;
	 }
    else
    {
    	bonus =1000;
	}
    printf (" enter name =% &" name);
    "(\n basic nd=%d", salary);
    "(\n basic=%d",bonus);
}

